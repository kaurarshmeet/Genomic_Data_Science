# Ignore all files and directories in the command_line_tools directory
command_line_tools/**

# But do not ignore the directory itself
!command_line_tools/

# Exclude the documents subdirectory from being ignored
!command_line_tools/documents/
!command_line_tools/documents/**

# Ensure .gitignore file in command_line_tools is not ignored
!command_line_tools/.gitignore
